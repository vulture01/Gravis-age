/**
 * lucide-react's current major version dropped brand/logo icons, so this
 * is a small hand-drawn glyph in the same stroke style as the rest of the
 * icon set, used anywhere we'd otherwise reach for lucide's Instagram icon.
 */
export default function InstagramIcon({ className = "h-5 w-5" }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden="true"
    >
      <rect x="3" y="3" width="18" height="18" rx="5" />
      <circle cx="12" cy="12" r="4" />
      <circle cx="17.2" cy="6.8" r="0.6" fill="currentColor" stroke="none" />
    </svg>
  );
}
